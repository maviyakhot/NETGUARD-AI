
# NetGuard AI
AI-Based Network Problem Detection and Offline Assistant.

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

CSV columns:
timestamp, latency_ms, packet_loss_pct, bandwidth_util_pct, cpu_usage_pct, memory_usage_pct
